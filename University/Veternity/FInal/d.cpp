#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    vector<double> xa(n), ya(n), xb(m), yb(m);
    for (int i = 0; i < n; i++) {
        cin >> xa[i] >> ya[i];
    }

    for (int i = 0; i < m; i++) {
        cin >> xb[i] >> yb[i];
    }

    // kasus 1 a dalam b;
    double hasil1 = 0;
    for (int i = 0; i < n; i++) {
        double jarak_maks = -1;
        for (int j = 0; j < n; j++) {
            
        }
    }
}