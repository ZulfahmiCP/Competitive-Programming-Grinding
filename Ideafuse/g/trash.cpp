
    // auto DEB = [&](node x) -> void {
    //     deb(x.sum[0], x.sum[1], x.pref, x.suff, x.p, x.s, x.whole, x.emp);
    //     newl;
    // };

    // // deb(tree.pos);
    // // deb(tree.head);
    // // deb(tree.val);
    // node x = node(1); 
    // node y = node(1);
    // node z = node(1);
    // node w = node(0); 

    // x = pull(x, y); 
    // deb(x.sum[0], x.sum[1], x.pref, x.suff, x.p, x.s, x.whole, x.emp);
    //     newl;
    // z = pull(z, w);
    // deb(z.sum[0], z.sum[1], z.pref, z.suff, z.p, z.s, z.whole, z.emp);
    //     newl;
    // x = pull(x, z);
    // deb(x.sum[0], x.sum[1], x.pref, x.suff, x.p, x.s, x.whole, x.emp);
    //     newl;